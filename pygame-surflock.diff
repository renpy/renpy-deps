--- /tmp/pygame-1.8.1release/src/surflock.c	2008-07-07 01:57:32.000000000 -0400
+++ surflock.c	2009-04-14 15:08:19.000000000 -0400
@@ -88,7 +88,8 @@
         return 0;
     }
     PyList_Append (surf->locklist, ref);
-
+    Py_DECREF(ref);
+    
     if (surf->subsurface)
         PySurface_Prep (surfobj);
     if (SDL_LockSurface (surf->surf) == -1)
