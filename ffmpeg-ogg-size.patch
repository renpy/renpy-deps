--- libavformat/oggdec.c.old	2009-04-09 04:29:19.000000000 -0400
+++ libavformat/oggdec.c	2009-04-09 04:31:02.000000000 -0400
@@ -450,7 +450,7 @@
     size = url_fsize(s->pb);
     if(size < 0)
         return 0;
-    end = size > MAX_PAGE_SIZE? size - MAX_PAGE_SIZE: size;
+    end = size > MAX_PAGE_SIZE? size - MAX_PAGE_SIZE: 0;
 
     ogg_save (s);
     url_fseek (s->pb, end, SEEK_SET);
